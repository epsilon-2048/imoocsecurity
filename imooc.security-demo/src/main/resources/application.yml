spring:
  datasource:
    url: jdbc:mysql://localhost:3306/imooc_demo?characterEncoding=utf8&serverTimezone=GMT%2b8&useSSL=false&allowMultiQueries=true
    username: root
    password: chen
    driver-class-name: com.mysql.jdbc.Driver
# basic验证暂时关掉
#security:
#  basic:
#   enabled: false

imooc:
  security:
    browser:
      # 自定义登录页
      loginPage: /demo-signIn.html
      # 自定义跳转方式
      loginType: JSON
    code:
      image:
        # 图形验证码数字个数
        length: 6
        # 宽度 如果在页面请求地址中带有width参数，则将覆盖此配置
        width: 50
        # 高度 如果在页面请求地址中带有height参数，则将覆盖此配置
        height: 25
        # 要验证码验证的url地址，","分隔，","之间不要有空格
        # url: /user,/user/*
    #不需做登陆验证的url，","分隔，","之间不要有空格
    #permitUrls: /user/me,/user/*